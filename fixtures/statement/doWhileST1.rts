// Basic do-while statement test
let i = 0;
let sum = 0;

// Execute the loop at least once, then check the condition
do {
  sum = sum + i;
  i = i + 1;
} while (i < 5);

console.log("Sum using do-while: " + sum);
